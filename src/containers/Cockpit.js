import React from 'react';
import classes from './Cockpit.module.css';

const cockpit = () => {
	return (
        <div className={ classes.cockpit }>
            <h3>Cockpit component</h3>
        </div>
    );
};

export default cockpit;